// This is an example of a JavaScript function that gets called when the button is clicked
// You can replace this with your own functionality
function askNow() {
    alert("You clicked the Ask Now button!");
  }
  
  // Add an event listener to the button to call the askNow function when it's clicked
  const button = document.querySelector('button');
  button.addEventListener('click', askNow);
  